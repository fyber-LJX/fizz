import Home from './../pages/home/index';

const router = [
    {
        path: '/',
        component: Home,
        exact: true,
        layout: null,
        attr: [],
        children: []
    }
]

export default router;